NEXT FEATURES:

Current - Setup LIDAR for testing

Finish testing "scanOnce" functionality

Add more functionality to the sopas class so that we can access other functions. 

Compile and build FFmpeg

Build a frame class that builds frames for videos/streams

Output DGRAM stream to VLC with FFmpeg build library to do so. 

DONE:

(03/11/2020) Get image from TIM551 and copy that into the mock lidar